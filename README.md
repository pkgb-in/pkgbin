# pkgbin

## Local hosts setup

Add the following entries to your system hosts file:

```
127.0.0.1 npm.pkgbin.local
127.0.0.1 gems.pkgbin.local
127.0.0.1 pypi.pkgbin.local
```

On macOS, edit `/etc/hosts` with sudo.
